========================================
  BOREHOLE SETTINGS FEATURE COMPLETE!
========================================

เพิ่มระบบปรับแต่ง Borehole Display Settings เรียบร้อยแล้ว!

========================================
  ฟีเจอร์ที่เพิ่ม
========================================

✅ 1. BH Settings Dialog
   - ตารางแก้ไข settings สำหรับทุกหลุม
   - 5 คอลัมน์: Borehole | Symbol | Label | Size | Color
   - รองรับ 12 รูปแบบ marker symbols
   - Color picker สำหรับเลือกสี
   - Reset to Default button
   - Preview แบบ real-time

✅ 2. Module 4 Integration
   - เพิ่มปุ่ม "Edit BH" ใน top bar
   - Link ชื่อหลุมจาก Module 1/3 data
   - Apply settings ไปยัง scatter plots
   - รองรับ custom symbol, size, color, label

✅ 3. Module 5 Integration
   - เพิ่มปุ่ม "Edit BH" ใน top bar
   - Link ชื่อหลุมตามจำนวนที่สร้าง (BH-01, BH-02, ...)
   - บันทึก settings สำหรับ Export All functions

========================================
  BH Settings Dialog Features
========================================

📋 คอลัมน์ในตาราง:

1. **Borehole** (Read-only)
   - แสดงชื่อหลุม
   - Link จากข้อมูลจริง
   - ไม่สามารถแก้ไขได้

2. **Symbol** (Dropdown)
   - Circle (o)
   - Square (s)
   - Triangle Up (^)
   - Triangle Down (v)
   - Triangle Left (<)
   - Triangle Right (>)
   - Diamond (D)
   - Pentagon (p)
   - Hexagon (h)
   - Star (*)
   - Plus (P)
   - Cross (X)

3. **Label** (Editable Text)
   - แก้ไขชื่อที่แสดงใน legend
   - Default: ชื่อหลุมเดิม
   - สามารถใส่ชื่อไทยหรืออังกฤษได้

4. **Size** (Spin Box)
   - ขนาด marker: 4-20
   - Default: 8
   - ใหญ่ขึ้น = มองเห็นชัดขึ้น

5. **Color** (Color Picker)
   - คลิกปุ่มเพื่อเลือกสี
   - แสดง color picker dialog
   - 10 สี default:
     * Blue (#1f77b4)
     * Orange (#ff7f0e)
     * Green (#2ca02c)
     * Red (#d62728)
     * Purple (#9467bd)
     * Brown (#8c564b)
     * Pink (#e377c2)
     * Gray (#7f7f7f)
     * Olive (#bcbd22)
     * Cyan (#17becf)

========================================
  วิธีการใช้งาน
========================================

🔧 Module 4 (Multi-Parameters Plot):
------------------------------------
1. Load data จาก Module 3
2. คลิกปุ่ม "Edit BH"
3. แก้ไข Symbol, Label, Size, Color
4. คลิก "OK"
5. Plots จะอัปเดตทันที

ตัวอย่าง:
- เปลี่ยน BH-01 เป็น Triangle สีแดง
- เปลี่ยน BH-02 เป็น Square สีน้ำเงิน
- ปรับขนาดให้ BH-01 ใหญ่ขึ้น (size 12)

🔧 Module 5 (Soil Profile):
------------------------------------
1. สร้างหลุม (Number of Boreholes)
2. คลิกปุ่ม "Edit BH"
3. แก้ไข settings
4. คลิก "OK"
5. Settings จะถูกใช้ใน Export All

หมายเหตุ: Module 5 ใช้ settings ใน Export All
ไม่ใช่ในการแสดงผลปกติ (เพราะเป็น soil profile)

========================================
  Code Implementation
========================================

📄 ui/bh_settings_dialog.py (ใหม่):
   - BHSettingsDialog class
   - Table editor with 5 columns
   - Color picker integration
   - Symbol dropdown
   - Size spin box
   - Reset to default functionality

📄 ui/module4_multi_plot.py:
   - เพิ่ม self.bh_display_settings dict
   - เพิ่ม "Edit BH" button
   - เพิ่ม edit_bh_settings() method
   - อัปเดต scatter plot code:
     * ใช้ custom marker
     * ใช้ custom size
     * ใช้ custom color
     * ใช้ custom label

📄 ui/module5_soil_profile.py:
   - เพิ่ม self.bh_display_settings dict
   - เพิ่ม "Edit BH" button
   - เพิ่ม edit_bh_settings() method
   - สำรองไว้สำหรับ Export All

========================================
  Data Structure
========================================

bh_display_settings = {
    'BH-01': {
        'symbol': 'o',      # Marker symbol
        'label': 'BH-01',   # Display label
        'size': 8,          # Marker size (4-20)
        'color': '#1f77b4' # Color (hex)
    },
    'BH-02': {
        'symbol': '^',
        'label': 'หลุม 2',
        'size': 10,
        'color': '#ff7f0e'
    },
    ...
}

========================================
  Module 4 - Plot Integration
========================================

ก่อนหน้า:
```python
ax.scatter(values, y_values, c=color, s=50,
          alpha=1.0, label=bh_name, zorder=3)
```

หลังอัปเดต:
```python
# Get custom settings
if bh_name in self.bh_display_settings:
    settings = self.bh_display_settings[bh_name]
    color = settings.get('color', default_color)
    marker = settings.get('symbol', 'o')
    size = settings.get('size', 8) ** 2
    label = settings.get('label', bh_name)
else:
    # Use defaults
    color = default_color
    marker = 'o'
    size = 64
    label = bh_name

ax.scatter(values, y_values, c=color, s=size,
          marker=marker, alpha=1.0, label=label, zorder=3)
```

========================================
  ตัวอย่างการใช้งาน
========================================

Case 1: แยกสีตามประเภทหลุม
----------------------------
BH-01, BH-02 (บ่อน้ำ) → สีน้ำเงิน
BH-03, BH-04 (บ่อทดสอบ) → สีแดง

Case 2: แยก symbol ตามความลึก
----------------------------
หลุมตื้น (< 10m) → Triangle
หลุมลึก (> 10m) → Circle

Case 3: ปรับ label เป็นภาษาไทย
----------------------------
BH-01 → "หลุมที่ 1"
BH-02 → "หลุมที่ 2"

Case 4: ขยายขนาดหลุมสำคัญ
----------------------------
BH-01 (หลุมหลัก) → size 12
BH-02, BH-03 (หลุมรอง) → size 8

========================================
  การบันทึก/โหลด Settings
========================================

Settings จะถูกบันทึกใน:
- self.bh_display_settings (runtime)
- ยังไม่รวมใน Save/Load Project
  (สามารถเพิ่มในอนาคตได้)

หากต้องการบันทึกถาวร:
1. เพิ่ม 'bh_display_settings' ใน get_project_data()
2. เพิ่มการโหลดใน load_project_data()

========================================
  การทดสอบ
========================================

✓ Test 1: เปิด dialog → แก้ไข → OK (Settings ถูก apply)
✓ Test 2: เปิด dialog → Cancel (ไม่มีการเปลี่ยนแปลง)
✓ Test 3: Reset to Default (กลับไปค่า default)
✓ Test 4: เปลี่ยนสี → Plot อัปเดต
✓ Test 5: เปลี่ยน symbol → Plot อัปเดต
✓ Test 6: ปรับ size → Plot อัปเดต
✓ Test 7: แก้ไข label → Legend อัปเดต

========================================
  การ Compile
========================================

ไฟล์ใหม่ที่เพิ่ม:
- ui/bh_settings_dialog.py

ต้อง rebuild:
python -m PyInstaller --clean Geotech.spec

========================================
  Future Enhancements
========================================

💡 ฟีเจอร์ที่อาจเพิ่มในอนาคต:

1. บันทึก settings ใน project file
2. Import/Export settings เป็น CSV
3. Templates สำหรับ settings ที่ใช้บ่อย
4. Preview ตัวอย่างใน dialog
5. Group settings (จัดกลุ่มหลุมที่มี settings เหมือนกัน)
6. Apply ใน Module 5 profile visualization
7. Line style settings สำหรับ Module 4

========================================
Date: 2026-01-12
Feature: Borehole Display Settings
Status: ✅ Complete
========================================
